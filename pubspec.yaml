name: nebula_ui
description: "Nebula UI — A token-driven Flutter Design System with space-inspired aesthetics, layered architecture, and responsive scaling."
version: 0.1.0
repository: https://github.com/nayadev/nebula_ui
issue_tracker: https://github.com/nayadev/nebula_ui/issues

topics:
  - design-system
  - ui
  - tokens
  - theme

environment:
  sdk: ^3.11.1
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
