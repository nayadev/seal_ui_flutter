# SealTextarea — With Label

Add a descriptive `label` above the text area by passing the optional
`label` parameter.

## Usage

```dart
SealTextarea(
  label: 'Message',
  hint: 'Type your message here…',
  controller: _controller,
  onChanged: (v) => _onChanged(v),
)
```
