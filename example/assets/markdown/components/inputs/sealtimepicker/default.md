# SealTimePicker — Default

24-hour time input with hour, minute, and second fields.

## Usage

```dart
SealTimePicker(
  onChanged: (time) => print('${time.hour}:${time.minute}:${time.second}'),
)
```
