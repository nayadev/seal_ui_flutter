# SealCalendar — Multiple

Picks multiple independent dates using `SealCalendar.multiple`.

## Usage

```dart
SealCalendar.multiple(
  selected: _dates,
  onChanged: (dates) => setState(() => _dates = dates),
)
```
