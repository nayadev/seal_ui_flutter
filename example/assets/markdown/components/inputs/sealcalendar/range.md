# SealCalendar — Range

Picks a continuous date range using `SealCalendar.range`.

## Usage

```dart
SealCalendar.range(
  selected: _range,
  onChanged: (range) => setState(() => _range = range),
)
```
