# SealFilledButton — Accent Secondary

A filled button using the **accent secondary** color. Use for supporting actions that complement the primary flow.

## Usage

```dart
SealFilledButton.accentSecondary(
  label: 'Confirm',
  onPressed: () {},
);
```

## Loading state

```dart
SealFilledButton.accentSecondary(
  label: 'Confirming…',
  isLoading: true,
  onPressed: null,
);
```

## Disabled state

```dart
SealFilledButton.accentSecondary(
  label: 'Confirm',
  onPressed: null,
);
```
