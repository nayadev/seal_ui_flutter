# SealFilledIconButton — Accent Gradient

A filled icon-only button with an **accent gradient** background.

## Usage

```dart
SealFilledIconButton.accentGradient(
  icon: Icons.bolt_rounded,
  tooltip: 'Boost',
  onPressed: () {},
);
```
