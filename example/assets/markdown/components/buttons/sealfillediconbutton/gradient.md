# SealFilledIconButton — Gradient

A filled icon-only button with a **gradient** background.

## Usage

```dart
SealFilledIconButton.gradient(
  icon: Icons.rocket_launch_rounded,
  tooltip: 'Launch',
  onPressed: () {},
);
```
