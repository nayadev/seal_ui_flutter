# NebulaIconButton — Accent Gradient

A transparent icon button with an **accent gradient** foreground tint.

## Usage

```dart
NebulaIconButton.accentGradient(
  icon: Icons.search_rounded,
  tooltip: 'Search',
  onPressed: () {},
);
```
