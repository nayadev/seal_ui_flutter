# SealIconButton — Accent Gradient

A transparent icon button with an **accent gradient** foreground tint.

## Usage

```dart
SealIconButton.accentGradient(
  icon: Icons.search_rounded,
  tooltip: 'Search',
  onPressed: () {},
);
```
