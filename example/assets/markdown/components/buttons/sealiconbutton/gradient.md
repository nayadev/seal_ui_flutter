# SealIconButton — Gradient

A transparent icon button with a **gradient** foreground tint.

## Usage

```dart
SealIconButton.gradient(
  icon: Icons.tune_rounded,
  tooltip: 'Filter',
  onPressed: () {},
);
```
