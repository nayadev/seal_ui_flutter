# NebulaOutlineIconButton — Primary

An outlined icon-only button with a **primary** color stroke. Use when a ghosted style is preferred for icon actions.

## Usage

```dart
NebulaOutlineIconButton.primary(
  icon: Icons.share_rounded,
  tooltip: 'Share',
  onPressed: () {},
);
```

## Disabled state

```dart
NebulaOutlineIconButton.primary(
  icon: Icons.share_rounded,
  tooltip: 'Share',
  onPressed: null,
);
```
