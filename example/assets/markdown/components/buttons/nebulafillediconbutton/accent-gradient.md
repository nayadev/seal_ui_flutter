# NebulaFilledIconButton — Accent Gradient

A filled icon-only button with an **accent gradient** background.

## Usage

```dart
NebulaFilledIconButton.accentGradient(
  icon: Icons.bolt_rounded,
  tooltip: 'Boost',
  onPressed: () {},
);
```
