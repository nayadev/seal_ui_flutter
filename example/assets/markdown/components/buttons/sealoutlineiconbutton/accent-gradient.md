# SealOutlineIconButton — Accent Gradient

An outlined icon-only button with an **accent gradient** stroke style.

## Usage

```dart
SealOutlineIconButton.accentGradient(
  icon: Icons.flash_on_rounded,
  tooltip: 'Boost',
  onPressed: () {},
);
```
