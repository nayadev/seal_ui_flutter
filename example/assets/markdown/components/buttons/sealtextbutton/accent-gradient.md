# SealTextButton — Accent Gradient

A text-only button rendered with an **accent gradient** foreground.

## Usage

```dart
SealTextButton.accentGradient(
  label: 'Explore',
  onPressed: () {},
);
```
