# NebulaOutlineButton — Primary

An outlined button with a **primary** color stroke. Use as a secondary action alongside a filled primary button.

## Usage

```dart
NebulaOutlineButton.primary(
  label: 'Cancel',
  onPressed: () {},
);
```

## Loading state

```dart
NebulaOutlineButton.primary(
  label: 'Cancelling…',
  isLoading: true,
  onPressed: null,
);
```

## Disabled state

```dart
NebulaOutlineButton.primary(
  label: 'Cancel',
  onPressed: null,
);
```
