# NebulaTextButton — Accent

A text-only button with the **accent** color. Use for low-emphasis accent-themed links or inline CTAs.

## Usage

```dart
NebulaTextButton.accent(
  label: 'Details',
  onPressed: () {},
);
```
