# NebulaFilledButton — Accent Gradient

A filled button with an **accent gradient** background. Use to draw maximum visual attention to a feature-highlighting action.

## Usage

```dart
NebulaFilledButton.accentGradient(
  label: 'Boost',
  onPressed: () {},
);
```

## Loading state

```dart
NebulaFilledButton.accentGradient(
  label: 'Boosting…',
  isLoading: true,
  onPressed: null,
);
```
