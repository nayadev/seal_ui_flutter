# SealProgress — Determinate

A token-driven linear progress bar built on `ShadProgress`. Pass a `value`
between `0.0` and `1.0` for a determinate bar.

## Usage

```dart
SealProgress(value: 0.6)
```

## Accent color

```dart
SealProgress(value: 0.4, useAccent: true)
```
