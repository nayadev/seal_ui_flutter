# SealProgress — Indeterminate

Omit `value` (or pass `null`) to show an animated indeterminate bar,
useful when progress cannot be measured.

## Usage

```dart
const SealProgress()
```

## Accent color

```dart
const SealProgress(useAccent: true)
```
